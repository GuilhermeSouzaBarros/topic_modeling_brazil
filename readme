python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements
python3 -m spacy download en_core_web_sm

python3 download.py         // skip if you already have the txts
python3 preprocesser.py
phthon3 main.py <num_topics> <num_words_per_topic>
